version: "3"

services:
  server:
    build: server/
    command: python server.py
    volumes:
      - ./server:/server

  client:
    build: client/
    # command: python client.py
    # Test
    command: tail -f /dev/null
    #command: python client.py
    volumes:
      - ./client:/client
    depends_on:
      - server